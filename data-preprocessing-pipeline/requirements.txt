# MM-MARAS Preprocessing Pipeline — Dependencies
# Install with: pip install -r requirements.txt

# Core scientific stack
numpy>=1.24
scipy>=1.10
xarray>=2023.1
pandas>=2.0

# NetCDF / HDF5 I/O
netCDF4>=1.6
h5py>=3.8

# GeoTIFF support
rioxarray>=0.14
rasterio>=1.3

# Zarr (fast array storage for patches)
zarr>=2.16

# Regridding (conservative method — optional but recommended)
# xesmf>=0.7  # requires separate conda install; see https://xesmf.readthedocs.io

# Copernicus Marine Service download toolbox
copernicusmarine>=1.0

# Progress bars
tqdm>=4.65

# Visualization (optional, for QA plots)
matplotlib>=3.7
cartopy>=0.21
